# Required by compose.yml (referenced as ${VAR} with no default).
# Lint rule L007 fails the deploy if any of these are absent here.
# Copy to .env (gitignored) and fill real values before deploying.

PUBLIC_HOSTNAME=crm.prvms.ru
SECRET_KEY=CHANGE_ME_64_RANDOM_CHARS
FIELD_ENCRYPTION_KEY=CHANGE_ME_FERNET_KEY
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
REDIS_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# Optional (have :- defaults in compose.yml; listed for discoverability):
# DB_NAME=platform_db
# DB_USER=platform
