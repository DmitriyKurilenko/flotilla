# echo-server has no required environment variables. This file exists
# so the convention is visible; copy it to .env (which is gitignored)
# before `flotilla deploy`.
#
# Optional (set in compose.yml; listed for discoverability):
# PORT=80
